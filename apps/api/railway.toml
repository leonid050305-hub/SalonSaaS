[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate"

[deploy]
startCommand = "npx prisma migrate deploy && node --import tsx/esm src/index.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
